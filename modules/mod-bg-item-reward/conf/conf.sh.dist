#!/usr/bin/env bash

## CUSTOM SQL - Important file used by the db_assembler.sh
## Keep only the required variables (base sql files or updates, depending on the DB)

## BASE SQL

DB_AUTH_CUSTOM_PATHS+=(
    "$MOD_BG_ITEM_REWARD_ROOT/sql/db-auth/base/"
)

DB_CHARACTERS_CUSTOM_PATHS+=(
    "$MOD_BG_ITEM_REWARD_ROOT/sql/db-characters/base/"
)

DB_WORLD_CUSTOM_PATHS+=(
    "$MOD_BG_ITEM_REWARD_ROOT/sql/db-world/base/"
)

## UPDATES

DB_AUTH_UPDATES_PATHS+=(
    "$MOD_BG_ITEM_REWARD_ROOT/sql/db-auth/updates/"
)

DB_CHARACTERS_UPDATES_PATHS+=(
    "$MOD_BG_ITEM_REWARD_ROOT/sql/db-characters/updates/"
)

DB_WORLD_UPDATES_PATHS+=(
    "$MOD_BG_ITEM_REWARD_ROOT/sql/db-world/updates/"
)
